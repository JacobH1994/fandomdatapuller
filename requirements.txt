# Phase 1 (collector) + Phase 2 (schema/ETL/Liquipedia connector).
httpx>=0.27
pyyaml>=6.0
mwparserfromhell>=0.6
pytest>=8.0

# notebooks/ (PRD §8) — reconciliation and other ad-hoc analysis notebooks.
pandas>=2.2
jupyter>=1.0
matplotlib>=3.8
scipy>=1.11
ruptures>=1.1
seaborn>=0.13
plotly>=6.0
kaleido>=0.2  # static PNG export for plotly figures, so notebook outputs are portable (render in GitHub's preview, not just a live Jupyter kernel) -- same reasoning every other notebook already embeds matplotlib PNGs, not interactive widgets
markdown>=3.5  # scripts/build_client_report.py -- renders clean report-body markdown to HTML for the Boudica PDF report pipeline

# anthropic (LLM-derived tournament aliases, etl/generate_tournament_aliases.py)
# is deliberately NOT listed here. This file is installed as-is by
# .github/workflows/poll.yml's hourly Twitch collector run (CLAUDE.md's
# "one rule": that workflow must never be put at risk) — confirmed live
# (2026-09-09) that installing anthropic alongside httpx pulls in a
# conflicting h11 version (pip flags it; a direct httpx request still
# worked in this environment, but "probably fine" isn't the bar for a
# shared file the collector's CI depends on). Install it separately,
# on-demand, only where alias generation actually runs:
#   pip install anthropic>=0.40
# That script already degrades gracefully (rule-based aliases only, no
# crash) if it's absent or ANTHROPIC_API_KEY is unset.
